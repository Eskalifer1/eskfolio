import { Section, SECTION_CONFIG } from "../sections";

export const navItems: Section[] = [
  SECTION_CONFIG.hero.key,
  SECTION_CONFIG.about.key,
  SECTION_CONFIG.skills.key,
  SECTION_CONFIG.projects.key,
];
